#include <stdio.h>

int main() {
    unsigned int n;
    scanf("%u", &n);

    unsigned int result = 0;

    for (int i = 0; i < 32; i++) {
        result = (result << 1) | (n & 1);
        n = n >> 1;
    }

    printf("%u\n", result);

    return 0;
}