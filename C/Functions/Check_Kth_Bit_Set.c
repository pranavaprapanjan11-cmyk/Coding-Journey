#include <stdio.h>
int main(){
    int n;
    scanf("%d",&n);
    int k;
    scanf("%d",&k);
    if(n>0&&(1<<k)){
        printf("%d bit is a set",k);

    }
    else{
        ("%d bit is not a set",k);
    }
}
